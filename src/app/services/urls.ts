export const serverUrl = 'http://localhost:8000';

export const loginUrl = `${serverUrl}/api/login`;
export const registerUrl = `${serverUrl}/api/register`;
export const logoutUrl = `${serverUrl}/api/logout`;

export const profileUrl = `${serverUrl}/api/profile`;

export const postsUrl = `${serverUrl}/api/posts`;
export const timelineUrl = `${serverUrl}/api/timeline`;
export const commentsUrl = `${serverUrl}/api/comments`;